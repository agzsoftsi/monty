#include "monty.h"
/**
 * queue - queue function
 * @stack: head
 * @num_linea: num linea
 * Return Nothing
 */

void queue(stack_t **stack, unsigned int num_linea)
{
	if (stack || num_linea)
		hola.flag = 0;
}
