#include "monty.h"

/**
 * nop - nop
 * @stack: node head
 * @num_linea: number of the line
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int num_linea)
{
	if (*stack)
	{};
	if (num_linea)
	{};
}
