#include "monty.h"
/**
 * stack - stack function
 * @stack: head
 * @num_linea: num linea
 * Return Nothing
 */

void stack(stack_t **stack, unsigned int num_linea)
{
	if (stack || num_linea)
		hola.flag = 1;
}
